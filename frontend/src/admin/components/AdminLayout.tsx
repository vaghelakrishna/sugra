import {NavLink,Outlet,useLocation} from 'react-router-dom'
import type {User} from '../types'
const links=[['/','◫','Overview'],['/products','◇','Products'],['/categories','□','Categories'],['/inventory','▤','Inventory'],['/orders','▱','Orders'],['/reviews','★','Reviews']]
export default function AdminLayout({user,onSignOut}:{user:User;onSignOut:()=>void}){const path=useLocation().pathname;const title=links.find(x=>x[0]===path)?.[2]||'Overview';return <div className="shell"><aside><div className="brand">Sugra<small>ADMIN PANEL</small></div><nav>{links.map(([to,icon,label])=><NavLink key={to} to={to} end={to==='/' }><span>{icon}</span>{label}</NavLink>)}</nav><div className="account"><i>{user.name[0]}</i><span>{user.name}<small>Administrator</small></span><button onClick={onSignOut}>↪</button></div></aside><main><header><div><small>STORE MANAGEMENT</small><h1>{title}</h1></div><i>{user.name[0]}</i></header><Outlet/></main></div>}
